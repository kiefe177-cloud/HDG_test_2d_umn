{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "C/C++: g++.exe build active file",
            "command": "C:/msys64/ucrt64/bin/g++.exe",
            "args": [
                "-fdiagnostics-color=always",
                "-g",
                // 1. Compile the active file (e.g., tests/test_jacobiP.cpp)
                "${file}",
                
                // 2. THIS IS THE FIX: Compile all implementation files in 'src'
                "${workspaceFolder}/src/*.cpp",

                "-o",
                "${fileDirname}\\${fileBasenameNoExtension}.exe",

                // 3. Include paths (Eigen and your own headers)
                "-I${workspaceFolder}/external/eigen-5.0.0",
                "-I${workspaceFolder}/external/HighFive",
                "-I${workspaceFolder}/include"
            ],
            "options": {
                "cwd": "C:/msys64/ucrt64/bin"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger."
        }
    ],
    "version": "2.0.0"
}
